{
  "name": "scale9x-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Scale9X Platform — one server, two portals, one shared database.",
  "scripts": {
    "start": "node --experimental-sqlite server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
